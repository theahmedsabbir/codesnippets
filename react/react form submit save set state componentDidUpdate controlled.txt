class NewBook extends Component {

    constructor(props) {
        super(props);

        // Defining State
        this.state = {
            book_name: "",
            author: "",
            description: "",
        };

        // attaching event for all functions
        // this.getInfo = this.getInfo.bind(this);
    }

    getInfo = (event) => {
        console.log(event.target.name);
        this.setState({
            // getting variable name dynamically
            [event.target.name]: event.target.value,
        });
    }

    // getting variable name dynamically
    saveBook = (event) => {
        event.preventDefault();
        console.log(event);
    }

    componentDidUpdate() {
        console.log(this.state);
    }

    render() {
        return (
            <div>
                <h1>New Book Entry</h1>
                <form onSubmit={this.saveBook}>
                    <div className='form-group'>
                        <label>Book Name</label><br />
                        <input type="text" onChange={this.getInfo} name="book_name" id="book_name" value={this.state.book_name} className='form-control' />
                    </div>
                    <div className='form-group'>
                        <label>Author</label><br />
                        <input type="text" onChange={this.getInfo} name="author" id="author" value={this.state.author} className='form-control' />
                    </div >
                    <div className='form-group'>
                        <label>Description</label><br />
                        <textarea onChange={this.getInfo} name="description" id="description" value={this.state.description} rows="5" className='form-control'></textarea>
                    </div >
                    <div className='form-group'>
                        <input type="submit" value="submit" />
                    </div>
                </form >
            </div >
        );
    }
}