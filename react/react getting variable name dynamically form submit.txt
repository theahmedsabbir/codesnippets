

    // getting variable name dynamically
    getInfo = (event) => {
        console.log(event.target.name);
        this.setState({
            [event.target.name]: event.target.value,
        });
    }